spring:
  application:
    name: poke-gateway-service   # 실제 애플리케이션 이름
  cloud:
    config:
      uri: http://poke-config-service:8888
      name: gatewayservice       # Config Server에 저장된 설정 이름
  profiles:
    active: dev